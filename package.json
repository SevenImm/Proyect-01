{
  "name": "scruffy-beards-team",
  "version": "1.0.0",
  "description": "This branch should only be used for approved code integration. This branch will be displayed as a page",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev":  "tailwindcss -i ./src/input.css -o ./dist/style.css --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.5"
  }
}
